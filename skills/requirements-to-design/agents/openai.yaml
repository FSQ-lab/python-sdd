interface:
  display_name: "Requirements To Design"
  short_description: "Turn requests into SDD design docs"
  default_prompt: "Use $requirements-to-design to clarify this Python change and produce a confirmed SDD design document."
policy:
  allow_implicit_invocation: false
