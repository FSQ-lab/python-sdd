interface:
  display_name: "Spec Driven"
  short_description: "Run SPEC-first implementation"
  default_prompt: "Use $spec-driven with the approved design document path to update SPEC.md, implement, verify, and audit."
policy:
  allow_implicit_invocation: false
